//! # mosaic-compositor
//!
//! The custom GPU compositor: scale + place + per-tile color convert + linear-light blend + overlay compositing.
//!
//! Scaffold crate — see `docs/` for the design. The library target is `mosaic_compositor`.
