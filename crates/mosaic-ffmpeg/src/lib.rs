//! # mosaic-ffmpeg
//!
//! Safe RAII wrappers over libav* (demux/decode/encode, hardware-frame lifecycle). FFI is behind the `ffmpeg` feature.
//!
//! Scaffold crate — see `docs/` for the design. The library target is `mosaic_ffmpeg`.
