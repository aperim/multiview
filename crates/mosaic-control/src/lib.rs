//! # mosaic-control
//!
//! The axum REST + WebSocket + SSE API: OpenAPI (utoipa+Scalar), auth, SQLite (sqlx), command-bus shell, embedded SPA serving. (HTTP/DB deps added during implementation.)
//!
//! Scaffold crate — see `docs/` for the design. The library target is `mosaic_control`.
