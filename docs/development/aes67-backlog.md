# AES67 / ST 2110-30 audio I/O — implementation backlog

Dependency-ordered, PR-sized slices. Reflects only what survived adversarial review.
Drives [aes67-delivery](../research/aes67-delivery.md) / [ADR-0033](../decisions/ADR-0033.md).
Sizes: `S` ≤½ day, `M` ~1–2 days, `L` ~3–5 days, `XL` multi-week (soak-gated).
TDD-first: failing test committed separately, then green without touching the test.

| # | Slice | Size | Crate(s) | Depends on | Deliverable + acceptance |
|---|---|---|---|---|---|
| **AES67-0** | f32↔Lk codec core | `S` | `multiview-output` (encode), reuse `multiview-input/st2110/v30.rs` (decode) | — | Pure encode: L16 `clamp·32767`→`i16`; L24 `clamp·8388607`→high 3 bytes of `(v<<8)`. **Property test:** ∀ f32∈[-1,1], encode→`V30Payload::parse` round-trips monotonic, **never sign-flips at full scale** (the `8388607` vs `8388608` guard). NIC-free. |
| **AES67-1** | `Aes67Packetizer` (RTP send framing) | `M` | `multiview-output` | AES67-0 | 12-byte RTP header (v2, dyn PT 96–127, random const SSRC, `seq.wrapping_add(1)`, **M=0 always**), per-packet `timestamp += groups`; regroup variable tick → fixed 48-group packets via the `AudioState` integer-FIFO **pattern**. **Test:** loopback to `V30Payload::parse` reconstructs the program block; M=0 on every packet; +48/packet. No `UdpSocket` yet. |
| **AES67-2** | `AudioStore::publish_at(frame)` | `M` | `multiview-audio` | — | Absolute placement honouring gaps/reorder; `read()` already silence-fills. **Property test:** out-of-order / lost RTP groups → silence at the correct absolute frame, **never a click, never a panic**; wait-free. |
| **AES67-3** | `Aes67AudioProducer` (receive wiring) | `M` | `multiview-input/st2110/` | AES67-2 | New audio `FrameProducer` (parallel to video `St2110Producer`): `RtpReceiver`→`V30Payload`→i32→f32 (L16 `/32768`, L24 `/8388608`)→`AudioBlock`→`publish_at`. Media-clock TS read at **48 kHz** (not 90 kHz). **Test:** loopback AES67-1 → producer → `AudioStore` matches source; reorder → silence-fill. |
| **AES67-4** | `channel_bridge` drop-oldest fix | `S` | `multiview-input/st2110/transport.rs` | — (precedes AES67-3 reuse) | Repair the doc-vs-code defect: on Full, **drop oldest** (drain+resend / ring), not the just-received newest. **TDD:** property test under sustained overflow asserts survivors are the **newest** units — write it failing first. |
| **AES67-5** | AES67-audio SDP build + parse | `M` | `multiview-input/st2110/sdp.rs` | AES67-3 | One parser+generator: `m=audio … RTP/AVP <pt>`, `a=rtpmap:<pt> L16\|L24/48000/<ch>`, `a=ptime`, `a=ts-refclk:ptp=…` (+ localmac form), `a=mediaclk:direct=<off>`. Reuse the `webrtc/sdp.rs` `RtpMap` **pattern**, not the module. IS-05 `transport_file` **delegates** here. **Test:** parse fills channels/depth/ptime/refclk/offset; generate round-trips; non-zero mediaclk offset applied on receive. |
| **AES67-6** | SAP announce + listen | `M` | `multiview-input/st2110/sap.rs` | AES67-5 | RFC 2974 UDP 9875, group **set** `{239.255.255.255, 224.2.127.254}` (default announce `239.255.255.255`, receiver joins both); ≥30 s AES67 cadence, 10×/1 h purge, `T`-bit delete. **Test:** announce/parse round-trip; purge after 10×; deletion removes the session. |
| **AES67-7** | PTP-anchored SEND timestamp | `M` | `multiview-engine` (consume `ptp.rs`), `multiview-output` | AES67-1 | RTP TS = PTP/TAI-absolute, `u128` or seconds/nanoseconds split (`secs*48000 + nanos*48000/1e9`) mod 2³², re-anchored per packet from `ptp.rs` (`timespec_to_ns` seam) — **not** the tick-keyed `SampleClock`. Dynamic TAI-UTC offset; leap = slew. **Property test:** `u128` form == `u64` split across full ns range; loopback round-trips a fixed PTP time to the expected receiver frame for a non-zero offset. |
| **AES67-8** | Boundary ppm resampler | `XL` | `multiview-audio` (or `-input`/`-output` seam), `rubato` | AES67-3, AES67-7 | Continuous fractional resampler driven by **measured PTP ppm**; pure-Rust `rubato` (not `ffmpeg`); identity when `Locked`; on receive sits **between depacketize and `publish_at`**. **Acceptance:** ADR-T006 **72 h soak + EBU R37**; inject a deliberate ppm offset → **zero gaps, zero audible drop/dup**. |
| **AES67-9** | `AlarmKind::ReferenceLoss` + fail-honest receiver | `M` | `multiview-core/alarm.rs`, `multiview-input`, `multiview-engine` | AES67-3, AES67-7 | New non-breaking `ReferenceLoss` variant + severity. Receiver: `Locked`/`Holdover` → last-disciplined origin; `Freerun` → wall-clock arrival placement, PTS marked estimated. **Test (linuxptp soft-GM + forced GM drop):** output frame count unchanged (inv #1), program **not muted**, `ReferenceLoss` raised/cleared, stream gap silence-fills. |
| **AES67-10** | PLC v1 — silence + edge fade ramp | `S` | `multiview-audio` | AES67-2 | Short fade ramp at conceal-span edges to kill the hard-silence click. **Test:** no step discontinuity at boundary; ramp length bounded. (Stateful interpolation PLC = v2, out of scope.) |
| **AES67-11** | `SourceKind::Aes67` config | `M` | `multiview-config/schema.rs:213` | AES67-3, AES67-5, AES67-6 | Internally-tagged `#[non_exhaustive]` variant (alias `st2110_audio`); binds by SDP/SDP-URL (T0) / SAP id (T1) / NMOS sender id (T2); carries group/port + channel/depth(L16\|L24)/ptime + `link_offset_ms` + PTP domain. Modelled on the NDI variant. **Test:** TOML+JSON round-trip; never untagged. |
| **AES67-12** | `Output::Aes67` config (first audio-only output) | `M` | `multiview-config/schema.rs:531`, `multiview-output` | AES67-1, AES67-7, AES67-11 | Internally-tagged `#[non_exhaustive]` variant, **no encode/gpu_pin** (raw PCM), reuses `OutputAudio` bus selector. Handle exhaustive `gpu_pin()`/`audio()` matches (always-`None` `gpu_pin` or explicit `Aes67 => None`). **Compile-test:** all `Output` variants reachable by both accessors. |
| **AES67-13** | Send isolation + chaos gate | `M` | `multiview-output`, `multiview-engine/pipeline.rs` | AES67-1, AES67-12 | Wire the sender into the off-hot-path bake consumer behind a bounded drop-oldest queue; include in `SINK_WEDGE_GRACE` teardown; never `send_to` on the output-clock loop. **Chaos test:** wedge the socket (full buffer/blackhole) → output clock keeps ticking, drop counters increment (template: `drain_is_bounded_and_never_awaits`). |
| **AES67-14** | Control-plane discovery + IS-04 resources | `M` | `multiview-control` | AES67-6, AES67-11, AES67-12 | Read-only `/api/v1` endpoint listing SAP + NMOS sessions; expose Sender/Receivers as IS-04 `MediaFormat::Audio` / `urn:x-nmos:transport:rtp.mcast`; tile-bind is **Class-1** (inv #11). **Test:** endpoint lists discovered sessions; bind classified Class-1. |
| **AES67-15** | NMOS-finish (audio transport_file + DNS-SD + activation bind) | `L` | `multiview-control/nmos/` | AES67-5, AES67-14 | Route the audio SDP `transport_file` through `st2110/sdp.rs` (DRY); DNS-SD over the wire; multicast bind on IS-05 activation. Sequenced **after** SAP/SDP; does **not** block plug-and-play. **Test:** IS-05 activation binds the receiver to the announced audio flow. |
| **AES67-16** | ST 2022-7 receive (optional) | `M` | `multiview-input/st2110/` | AES67-3 | Engage `DualPathPacketSource` + `HitlessReconstructor` for AES67-RX behind config; depacketize **downstream** of merge. **Guard:** validate both flows **share SSRC** and don't share *both* identical src AND dst addresses; reject otherwise. **Property test:** dual-path L16/L24 merge essence-agnostic (analogue of `st2022_7_properties.rs`). Send-side dual-NIC = **v2, deferred**. |

## Sequencing notes

- **Critical path:** AES67-0 → 1 → (2,3) → 5 → 6 → 11 → 12 → 13 → 14. AES67-7 (PTP send TS) joins before AES67-12/-13 ship a real sender.
- **Parallelizable:** AES67-2 (`publish_at`) and AES67-4 (drop-oldest fix) have no upstream deps; AES67-10 (PLC) after AES67-2; AES67-16 after AES67-3.
- **Long-pole:** AES67-8 (resampler) is the only `XL`/soak-gated item; locked operation works on the FIFO alone until it lands, so it does **not** gate first interop — only gates strict long-run drift conformance.
- **Optional tier:** AES67-15 (NMOS-finish) and AES67-16 (ST 2022-7 RX) are additive; neither blocks plug-and-play.
- **Out of scope:** Class B/C ptime (additive config after Class A), ST 2022-7 send, native Dante (per [ADR-T010](../decisions/ADR-T010.md)), AES3/2110-31 mixing levels (AX/BX/CX), stateful interpolation PLC.
