export default function App() {
  return (
    <main className="app">
      <h1>Mosaic</h1>
      <p>Management console — scaffold.</p>
      <p>
        See <code>docs/web/management-app.md</code> for the planned UI (dashboard,
        sources, the drag-and-drop layout editor, outputs/transcoding, preview).
      </p>
    </main>
  );
}
